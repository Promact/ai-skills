---
description: Use for React Native projects (package.json has a react-native dependency) — Promact best practices and coding guidelines for React Native
globs:
alwaysApply: false
---

Check the project's `package.json` for a `react-native` dependency before
applying — React Native shares file extensions with plain React, so this
can't be glob-matched reliably. Apply Promact's React Native conventions
below when writing or reviewing code.

@../../skills/best-practices/stacks/react-native.md
@../../skills/coding-guidelines/stacks/react-native.md

For cross-cutting concerns (REST API design, exception handling, security,
database access, naming, code organization, testing structure), see the
other rules in this directory — they apply regardless of stack.
